#include <cs50.h>
#include <stdio.h>
#include <ctype.h>

int main(int argc, char argv[])
{
    if (argc == 2 && isdigit(argv[1]))
    {
        
    }
    else
    {
        printf("Usage: %s key\n", argv[0]);
    }
}
